/*untuk mengambil atau melihat data , gunakan perintah select. select bisa digunakan untuk
menampilkan sebagian atau semua kolom yang ada di tabel. untuk mengambil beberapa kolom data
kita bisa sebutkan nama kolomnya satu-persatu. jika ingin mengambil semua datanya kita bisa 
menggunakan tanda * .

SELECT nama_kolom1, nama_kolom2 FROM nama_tabel;

SELECT * FROM nama_tabel;

contohnya disini kita akan mengambil data dari tabel 'contoh_tabel' yang memiliki 2 kolom 
didalamnya. */


SELECT kolom1 FROM contoh_tabel;

SELECT * FROM contoh_tabel;